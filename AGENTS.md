# Pebble 开发约定

本文件适用于整个 Pebble 仓库。以用户当前指令为准；与既有设计冲突时更新受影响的设计，不把历史提案当作不可变要求。

## 产品目标

Pebble 是 SAIR 面向科学研究的成果发现、复用与发布平台。核心体验是：找到一个结果，理解其条件，准确引用，将其作为依赖用于自己的研究，再发布可复用的新贡献。

- 本地工作流参考 Rust Cargo：包清单、依赖解析、锁文件、构建、检查、打包、分发与发布。
- 网站整体结构参考 Hugging Face Hub：领域目录、Research card、文件与版本、使用入口、研究者/组织和 Collections；保留 GitHub 熟悉的导航方式。
- 学术标题、作者、问题、结论、条件、引用与使用示例是页面中心。不要把 Pebble 做成只有文件树和提交记录的通用 Git 网站。
- 包是安装与交付单位，具体结果是发现、理解和引用单位。一个包可包含多个定理；一个引理也可作为包发布，不强制附论文。
- Research、Packages、Results 是同一发布内容的关联视图，不要求作者重复上传三份内容。
- 面向一般科研能力设计，先用实际形式化数学工作验证。不得把单个领域或示例硬编码成平台的特殊接受路径。

## 产品和仓库边界

| 系统 | 职责 |
| --- | --- |
| Pebble | 成果/包目录、注册与分发、版本、验证任务协调、发布、引用、定理 DAG、探索 API、权限及学术社区 |
| Slate | 语言、编译器、可信检查器、本地包管理客户端、依赖构建、证据重放与本地检查缓存 |
| Clay | 写作编辑器、实时共同编辑、科研 IDE、学习和交互式 agent 工作流 |

**写作协作归 Clay。Pebble 提前准备接入能力，但自身不提供写作协作功能。**

- 设计稳定的项目/包/结果/版本身份、客户端授权、成果快照提交、精确引用和检查/发布查询接口，供 Clay、CLI 和网页共用。
- Pebble 接收固定的论文、源码及数据成果版本。编辑器文档树、CRDT、光标、击键、实时草稿同步及合并属于 Clay，不在这里预建对应服务或表。
- 提交成果不自动公开、发布或贡献会话记录；分别执行相应动作和授权检查。
- Pebble 可以提供论文上传、阅读、下载、研究说明表单和讨论。不要把这些扩展成在线论文写作系统。
- 完整 Git 托管、复杂 PR 工作流、竞赛和训练数据管线按具体任务推进，不作为首个包管理闭环的前置条件。借鉴 HF 不意味着同时实现 Spaces、模型推理或独立数据集产品。
- 模型训练和推理服务属于独立系统；本仓库不因总体 SAIR 愿景自动扩大到这些实现。

## 定理 DAG 与 agent 探索

定理发现以 DAG 为核心组织方式。Pebble 提供图和工具接口，Clay 或外部 agent 自主决定如何探索，Slate 检查候选的实际使用。

- 节点绑定精确版本的声明、环境和所选证据。保留陈述、参数、前提、理论/假设、出处、引用及导入信息。
- 约定 `T → P` 表示 T 的所选已检查证明直接使用 P。支持正向依赖和反向使用者查询；反向邻接是查询视图，不是另一条证明依赖边。
- 直接边来自检查器实际依赖。不能通过源码 import、文件共现、论文引用、LLM 猜测或传递闭包伪造直接边。缺少导出信息时补充 Slate 接口，并如实标记缺口。
- 同名、相同命题或不同版本不自动合并节点、证明和署名。另一个依赖不同的证明须保留不同证据绑定；例行重检不必复制相同节点。
- 公理/原则叶节点明确标明种类；论文引用、符号关联、相似性和作者提出的推广关系另存。这些关联可能有环，不宣称它们全是证明 DAG。
- 区分定理依赖 DAG、单个证明内部 DAG 与包依赖图。局部证明步骤不自动成为可独立导入的全球定理。
- 提供入口查找、批量节点读取、分页双向邻接及按需证明片段。节点摘要先行，详细条件与证明按需获取，不把整个图塞进模型上下文。
- 名称、文本、符号和包/领域目录用于寻找多个入口；向量检索和 reranker 是可评测的增强，不是主路径的前置条件。不要默认从基础公理开始全图遍历。
- agent 探索有节点、边、字节、调用和时间预算，并保留已访问节点及候选试用上下文。预算耗尽或一次尝试失败不能推断相关定理不存在。
- 验证端点、证据绑定和无环性，完整子图准备好后才提供相应图视图。不能通过删边掩盖提取错误。
- 先用直接边和正反向邻接实现图访问。避免展开所有路径、重复遍历共享子图或无需求地引入专用图数据库。

## 包、版本和发布

- 明确区分作者依赖意图、生成的精确锁定图、不可变包快照、检查记录和正式发布。名字、slug、版本号、内容摘要与验证状态不是同一个身份。
- 清单/锁文件与本地 resolver 在 Slate 实现；Pebble 提供注册协议和元数据。不要在网页服务中另写一套与客户端冲突的依赖语义。
- 接入前核查实际 Slate 提交与接口。设计文档中的命令、schema 和历史源码基线不代表当前已实现或稳定支持。
- 同一个包版本不能覆写。修订内容产生新版本；撤回和验证撤销是附加状态，保留精确引用落点。
- 源码、数据、依赖、工具链与相关政策都要固定。不要在验证期间依赖可变标签、未锁定下载或任意宿主环境。
- 保留精确 ModuleId 和环境绑定。不得仅改变安装目录就宣称同名模块多版本可共存，也不得重新解析传递依赖后继续沿用旧结果身份。
- 所有摘要由工具从实际内容生成，不复制当前输出手工维护。避免清单自含摘要或包/发布之间的 hash cycle。
- 包快照可独立上传；不要求作者先在 Pebble 创建 Git 仓库。外部 Git 地址/commit 是来源信息，不能代替实际内容校验。
- 发布内容先准备，再在事务中复查输入、权限、验证、审核和版本唯一性，原子建立正式记录；重试幂等，不暴露部分发布。

## 数学验证与展示边界

- Slate 决定数学接受。Pebble 的工作是准确选择输入、协调检查、保存证据关联并执行发布要求，不新增内核规则或另一个证明格式。
- 正式发布需要完整的形式化源码分类与检查覆盖。仅检查作者挑选的一个定理、文件或主入口不足够；不支持、未解决或漏检内容不能获得完整通过状态。
- `ProvedStatement`、程序构建成功、ordinary execution、检查缓存命中、元数据审核和正式发布分别表达。不能抓一次 stdout 成功字符串就给整包标绿。
- 开发假设不得进入正式验证发布。字段缺失不能推断为 release eligible；普通程序运行成功不能代替正确性证明。
- 保留明确的理论、公理、经典原则及其他假设。要求显式披露，不要求所有合法研究都具有空假设闭包。
- 精确形式化命题、论文中的自然语言主张与科学新颖性分别评价。基本提交审核不评判科学重要性；理论物理推导被检查不意味着其物理假设已获实证确认。
- 下载缓存与本地检查器生成的受信任缓存分开。外来的 `.slatecache`、自洽 hash 或作者签名不能自动赋予数学接受权。
- 检索相关性、结构匹配与实际形式检查分开。DAG 邻接或 agent 判断都不能直接生成已证明状态。
- 不可信代码与检查结果收集权限隔离；用户构建任务不能写正式发布记录、其他任务的信任缓存或伪造受控检查输出。

## 学术归属、权限和研究记录

- 分别记录原结果作者、形式化贡献者和包维护者；区分已有结果的形式化与作者声称的新成果，不从 Git commit 数推断学术贡献。
- 科学引用、直接形式依赖和传递依赖分别保存、展示和计数。相同内容重新发布不能凭索引去重夺走原作者署名。
- 项目访问、研究记录公开与训练贡献是独立维度。私有项目不自动代表退出训练；涉及 Clay 记录接入时遵循愿景中的默认贡献、退出选择和显著提醒。
- 训练规则不意味着可以绕过来源权利。记录贡献者、第三方内容和 BYOK 输出的权限；不确定是否可训练的内容不进入导出。
- 成果提交不顺带收集写作历史或对话。受保护评估题、变体和问题族与训练导出隔离。
- API、图邻接、搜索片段/计数、下载和后续通知都检查当前读取权限。已知对象摘要或固定图快照不等于读取授权。
- 把不可变内容和可撤销的访问/验证状态分开；不能等待异步索引清理才阻止泄漏或误报。

## 实施方法

先读 [README](README.md)，再根据当前任务读取相关设计：

- [产品定位与 Hub 结构](docs/product-architecture.md)
- [愿景需求及用户范围修订](docs/requirements.md)
- [技术方案和 Clay 接入边界](docs/technical-research.md)
- [包管理、验证和发布协议](docs/package-verification.md)
- [定理 DAG 与 agent 探索](docs/theorem-index.md)

这些设计中的技术栈、schema 和命令提案不自动成为已实现能力或已冻结契约。当前代码状态以仓库为准。仓库级原则放在本文件，格式细节和实验数据留在对应文档，避免重复维护一份实现清单。

- 沿用户指定任务完成具体闭环，不以持续增加设计文档、空壳 API 或 UI 样机替代已授权的实现。
- 选择最小但真实的端到端工作：实际包、真实 Slate 检查、干净客户端复用或真实 DAG 遍历。测试样例与生产状态分离，不能以模拟验证成功填补未实现接口。
- 通用软件组件尽量复用；技术选型按真实需求和实验结果调整。不预建全部未来服务，也不保留没有使用者的兼容层。
- 对图探索的比较固定模型、目标、入口及总预算，测真实使用成功率和访问成本。不能用更多推理开销冒充方法改进。
- 测试匹配变更风险：依赖替换、版本冲突、漏检、缓存来源、图环/缺边、权限撤销、重复任务及发布原子性按受影响路径验证。文档修改检查一致性和链接即可，不跑无关编译器全套测试。
- 更新相关文档时区分事实、提案、已实现行为与实测结果；不声称未执行的检查通过。

## 工作区与交付

- 先检查当前分支、工作树与远端基线。新的独立工作按用户要求从最新 main 开分支；已有任务继续其分支，不在 main 直接开发。
- Pebble 与 Slate 是独立仓库。不要误改旧 Loom/Slate 工作树，不清理或覆盖其他 agent/用户的未提交内容；跨仓库改动明确说明归属和基线。
- 遵循已有授权推进工作，不为日常可逆编辑反复请求确认。推送、合并和部署是不同动作；推送分支不表示获准合并或上线。
- 默认用中文沟通，代码标识使用清晰的英文。完成时说明实际变化、相关验证和剩余缺口，明确本地提交、远端推送或已合并的真实状态。
